#include <iostream>
#include <string>
#include <cmath>
#include <algorithm>

std::string determine_pattern(const std::string &text, int size) {

  int lenght = text.size();

  auto val = std::pow(2,3);

  std::cout << val;

  return {};



}

int main() {

  std::string s = "aba";

  do std::cout << s << '\n';
  while (std::next_permutation(s.begin(), s.end()));
  std::cout << s << '\n';
}
